import React from 'react';

import { LoadingStyled as Styled } from './loading.styles';

export const Loading = () => {
  return (
    <Styled.Container>
      <Styled.Text>Loading.....</Styled.Text>
    </Styled.Container>
  );
};
